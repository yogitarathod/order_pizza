import React from "react";
import { Container } from "react-bootstrap";

const StopOrder = (props) => {
    return <>
        <Container className="mt-5 text-center">
            <h2>Not Taking any order for now</h2>
        </Container>
    </>
}

export default StopOrder